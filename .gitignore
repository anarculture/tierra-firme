node_modules/
.venv/
.env
.env.*
!.env.example
data/bundles/
data/sheets/*.csv
data/sitrep-drafts.json
# libro interno (ADR 0006): necesidades/compras/entregas + costos + foto-comprobante = INTERNO
data/libro.json
dist/
*.log
.DS_Store
.claude/

# PII / tooling — repo público, nunca pushear
.codegraph/
docs/WhatsApp Chat*/
# snapshots de ingesta con PII — locales, alimentan el API (redactado/gated al servir)
data/missing.json
data/patients.json
data/puntos_ayuda_live_db.csv
data/*.csv
data/reportes/
data/imported/hospitales-sismo/*.json
data/imported/hospitales-sismo/*.csv
# descarga cruda de Drive "SISMO 2026 VZLA" (fotos de listas + xlsx con PII)
data/SISMO 2026 VZLA*
# salida de analiza.js — el piiScan deja pasar PII de terceros (emails/nombres citados); nunca trackear cruda
data/analisis-*.json
# dir de trabajo del ejercicio de limpieza: export WhatsApp (PII) + media estancada para VLM
cleaner/

# OpenWA/venom browser session (retirado; nunca trackear)
ingest/.wa-session/
