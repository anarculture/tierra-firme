/* Fuente: AyudaVE (ayudahumanitariavenezuela.com) — personas, centros, hospitalizados.
   Expone /api (JSON): /centers, /persons, /hospitals. Read-only. */
/** @returns {Promise<import("../model/index.js").Registro[]>} */
export async function fetchRegistros() {
  // TODO(Sx): GET /api/persons + /api/hospitals + /api/centers → normalizar a Registro[].
  return [];
}
