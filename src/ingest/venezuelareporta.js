/* Fuente: Venezuela Reporta (venezuelareporta.org) — AGREGADOR MAESTRO de personas.
   Posicionamiento (Q7): FEDERAR, no re-agregar. Contacto: hola@buscados.org. Read-only.
   Preferir feed/federación acordada antes que scrape (ver plan §Ética/permiso). */
/** @returns {Promise<import("../model/index.js").Registro[]>} */
export async function fetchRegistros() {
  // TODO(Sx): integrar vía federación (feed acordado) → Registro[] categoria 'persona'.
  return [];
}
