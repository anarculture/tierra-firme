/* Fuente: Acopio Venezuela (acopiovenezuela.vercel.app) — centros de acopio (~158).
   Exposición: scrape/JSON embebido (Next.js). Read-only. */
/** @returns {Promise<import("../model/index.js").Registro[]>} */
export async function fetchRegistros() {
  // TODO(Sx): extraer centros → normalizar a Registro[] categoria 'centro'.
  return [];
}
